---
features:
  - A new recordset api ``/v2/recordsets`` is exposed with GET method
    only. The api can be used for retrieving recordsets across all the
    zones under a tenant. Filtering on certain fields are supported as well.